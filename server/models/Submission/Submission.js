const { Schema, model } = require('mongoose');

const submissionSchema = new Schema(
    {
        // author, title, id, timeStamp, type
        // type extends submission
        // bill {
        //     text
        //     party
        // }
        // campaignEvent {
        //     location
        //     
        // }
    }
)